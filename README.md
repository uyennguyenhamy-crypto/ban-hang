# Discord Shop Bot

Railway start command is defined in `Procfile`.

Required Railway Variables:
- TOKEN = Discord bot token
- GUILD_ID = Discord server ID (recommended for instant slash-command sync)

The bot creates `products.json` automatically when needed.
